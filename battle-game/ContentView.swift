import SwiftUI

/// Root view — boots straight into the main menu (Stage 1: menus + levels).
struct ContentView: View {
    var body: some View {
        MainMenuView()
    }
}

#Preview {
    ContentView()
}
